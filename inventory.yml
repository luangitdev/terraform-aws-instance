all:
  hosts:
    ec2_instance:
      ansible_host: "{{ instance_public_ip }}" # IP Público da instância
      ansible_user: ubuntu
      ansible_ssh_private_key_file: "/home/luan/aws/acesso-aws.pem"
